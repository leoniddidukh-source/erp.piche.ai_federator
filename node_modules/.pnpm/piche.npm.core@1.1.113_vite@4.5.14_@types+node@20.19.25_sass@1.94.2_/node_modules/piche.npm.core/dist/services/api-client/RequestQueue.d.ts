import type { IRequestQueueItem } from '../../types';
export declare class RequestQueue {
    private queue;
    private tripWire;
    enqueue: (item: IRequestQueueItem) => void;
    dequeue: () => IRequestQueueItem | undefined;
    get length(): number;
    get isTripWire(): boolean;
    set isTripWire(value: boolean);
}
