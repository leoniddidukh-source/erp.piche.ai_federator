import type { BridgeRequests, RequestHandler } from '../../types';
export declare function useBusRequest<K extends keyof BridgeRequests>(request: K, handler: RequestHandler<BridgeRequests[K]['request'], BridgeRequests[K]['response']>): void;
