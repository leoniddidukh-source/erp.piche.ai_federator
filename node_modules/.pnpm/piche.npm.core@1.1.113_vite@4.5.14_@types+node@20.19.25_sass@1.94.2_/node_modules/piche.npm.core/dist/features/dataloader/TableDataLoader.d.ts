import type { Subscriber } from '../../types';
type TableDataLoaderConfig = {
    maxBatchSize: number;
    debounceTime: number;
    cacheClearTime: number;
    apiTemplateUrl: string;
};
export declare class TableDataLoader implements Subscriber {
    private requestQueue;
    private configuration;
    private readonly tableCache?;
    constructor(configuration: TableDataLoaderConfig);
    subscribe(): void;
    request<T>(table: string, id: string): Promise<T>;
    private executeBatchRequest;
}
export {};
