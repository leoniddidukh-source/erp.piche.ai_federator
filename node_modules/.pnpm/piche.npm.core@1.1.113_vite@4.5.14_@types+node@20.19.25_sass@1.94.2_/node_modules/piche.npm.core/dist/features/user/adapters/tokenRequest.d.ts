import type { IIdentityService, IRestTokenResponse } from '../../../types';
export type TokenRequestParams = Omit<Record<string, string>, 'client_id'> & {
    grant_type: string;
};
export declare function requestToken(identityService: IIdentityService, realm: string, params: TokenRequestParams): Promise<IRestTokenResponse>;
