import type { BulkInsertResult, FilterParameters, GenericObjectType, OperationResult, PaginationType, Subscriber, Table, TableSingle } from '../../types';
export declare class ViewClient implements Subscriber {
    subscribe(): void;
    getAsync<T>(view: string, id: string): Promise<TableSingle<T>>;
    getAllAsync<T>(view: string, top?: PaginationType, skip?: number): Promise<Table<T>>;
    filterAsync<T>(params: Partial<FilterParameters>): Promise<Table<T>>;
    updateAsync(view: string, id: string, body: GenericObjectType): Promise<OperationResult>;
    insertAsync(view: string, body: GenericObjectType): Promise<OperationResult>;
    bulkInsertAsync(view: string, body: GenericObjectType[]): Promise<BulkInsertResult[]>;
    deleteOneAsync(view: string, id: string): Promise<OperationResult>;
    deleteManyAsync(view: string, records: Array<string>): Promise<OperationResult>;
    getUrl(view: string, id?: string): Promise<string>;
    private getDataOrThrowRecordNotFound;
    private insertRowsAsync;
    private formatInsertResults;
}
export declare const viewClient: ViewClient;
