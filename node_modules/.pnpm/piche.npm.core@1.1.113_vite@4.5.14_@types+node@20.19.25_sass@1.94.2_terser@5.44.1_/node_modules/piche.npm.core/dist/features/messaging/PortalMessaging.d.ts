import type { Consumer, IPortalMessageSubscription } from '../../types';
export declare class PortalMessage {
    static emitMessageAsync: <T>(eventName: string, message: T) => void;
    static subscribe: (eventName: string, listener: Consumer<CustomEvent>) => IPortalMessageSubscription;
}
