import type { ConfigProps, Subscriber } from '../../types';
export declare const requestApiConfigAsync: () => Promise<ConfigProps>;
export declare class AppConfig implements Subscriber {
    config: Partial<ConfigProps>;
    constructor(config: Partial<ConfigProps>);
    subscribe(): void;
}
