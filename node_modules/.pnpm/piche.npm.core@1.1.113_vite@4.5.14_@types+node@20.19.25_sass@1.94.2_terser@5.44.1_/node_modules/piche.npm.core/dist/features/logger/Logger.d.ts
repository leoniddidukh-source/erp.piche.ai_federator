import { Optional } from 'typescript-optional';
import type { ILoggerOptions, ILogMessage } from '../../types';
import { LogLevel } from '../../types';
export declare class Logger {
    private static WRITE_LOG_EVENT;
    private readonly loggerId;
    private options;
    private messages;
    private interval;
    private events;
    private lastTimeLogsWasSent;
    constructor(options: ILoggerOptions);
    getKey: (key: string) => Optional<string>;
    setKey: (key: string, value: string) => void;
    isConsoleOutputEnabled: () => boolean;
    enableRemoteLogging: () => Logger;
    log: (text: string, code: string, logLevel: LogLevel, error?: Error, nameSpace?: string) => ILogMessage;
    getStoredLogLevel: (key: string) => false | LogLevel;
    enableEventDrivenLogging: () => Logger;
    canWriteLog: (logMessageLogLevel: LogLevel, nameSpace: string) => boolean;
    private write;
    private handleConsole;
    private handleRemote;
    private resetLastTimeLogsWasSent;
    private getLogIdentity;
    private getLoggerId;
    private createLoggerId;
    private startLogSending;
    private checkForLogsToSend;
    private cloneLogMessages;
    private clearLogMessages;
    private isSendTimeReached;
    private isThereSomethingToSend;
}
