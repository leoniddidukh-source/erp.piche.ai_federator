import type { AxiosResponse } from 'axios';
import type { AxiosInstanceWithOperations, OperationResult, Subscriber, Supplier } from '../../types';
export interface IOperationClient {
    fetchOperationStatusAsync(operationId: string, apiVersion?: string): Promise<OperationResult>;
}
export declare const operationClient: () => Promise<IOperationClient>;
declare class OperationClient implements IOperationClient, Subscriber {
    private readonly url;
    constructor(operationUrl: string);
    subscribe(): void;
    private handleRetry;
    fetchOperationStatusAsync: (operationId: string, apiVersion?: string) => Promise<OperationResult>;
}
declare const waitForOperation: (axios: AxiosInstanceWithOperations, axiosAction: Supplier<Promise<AxiosResponse>>) => Promise<OperationResult>;
export { OperationClient, waitForOperation };
