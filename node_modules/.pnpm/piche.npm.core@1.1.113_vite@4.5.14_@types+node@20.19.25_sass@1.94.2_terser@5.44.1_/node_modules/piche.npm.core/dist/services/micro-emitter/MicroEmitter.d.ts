import type { ISubscription, Listener } from '../../types';
export declare class MicroEmitter {
    private listeners;
    addListener: (type: string, listener: Listener) => MicroEmitter;
    on: (type: string, listener: Listener) => MicroEmitter;
    subscribe: (type: string, listener: Listener) => ISubscription;
    addOnceListener: (type: string, listener: Listener) => MicroEmitter;
    once: (type: string, listener: Listener) => MicroEmitter;
    removeListener: (type: string, listener?: Listener) => MicroEmitter;
    off(type: string, listener: Listener): MicroEmitter;
    emit: (type: string, payload?: any) => MicroEmitter;
    private storeListener;
}
