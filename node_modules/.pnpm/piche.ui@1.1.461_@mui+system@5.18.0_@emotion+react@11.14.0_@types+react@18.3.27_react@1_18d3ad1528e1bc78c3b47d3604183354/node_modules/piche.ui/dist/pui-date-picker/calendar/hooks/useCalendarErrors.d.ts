import type { DateSlotKey } from '../types';
type ErrorState = {
    date: string;
    time: string;
};
export declare const useCalendarErrors: () => {
    errors: Record<DateSlotKey, ErrorState>;
    updateDateError: (key: DateSlotKey, error: string) => void;
    updateTimeError: (key: DateSlotKey, error: string) => void;
    resetErrors: () => void;
    clearDateError: (key: DateSlotKey) => void;
    clearTimeError: (key: DateSlotKey) => void;
    hasErrors: (key: DateSlotKey) => boolean;
    getDateError: (key: DateSlotKey) => string;
    getTimeError: (key: DateSlotKey) => string;
};
export {};
