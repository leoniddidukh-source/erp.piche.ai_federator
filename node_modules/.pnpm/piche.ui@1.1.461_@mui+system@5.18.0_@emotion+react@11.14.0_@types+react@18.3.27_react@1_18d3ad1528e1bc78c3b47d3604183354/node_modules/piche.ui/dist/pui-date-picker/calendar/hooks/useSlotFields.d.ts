import type { DateSlotKey } from '../types';
type FieldState = {
    date: string;
    time: string;
};
export declare const useSlotFields: () => {
    fields: Record<DateSlotKey, FieldState>;
    updateDateField: (key: DateSlotKey, date: string) => void;
    updateTimeField: (key: DateSlotKey, time: string) => void;
    resetFields: () => void;
    getDateField: (key: DateSlotKey) => string;
    getTimeField: (key: DateSlotKey) => string;
};
export {};
