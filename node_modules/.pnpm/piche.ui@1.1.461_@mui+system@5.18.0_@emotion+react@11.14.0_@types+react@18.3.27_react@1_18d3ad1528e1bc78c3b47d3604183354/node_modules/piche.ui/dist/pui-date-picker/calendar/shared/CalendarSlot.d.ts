import type { DateSlotKey } from '../types';
type CalendarSlotProps = {
    value: string;
    id: DateSlotKey;
    placeholder: string;
    time?: string;
    isActive?: boolean;
    onSelect?: (key: string) => void;
    onTimeChange?: (value: string) => void;
    onDateChange?: (value: Date) => void;
};
export declare const CalendarSlot: ({ isActive, time, value, id, placeholder, onDateChange, onSelect, onTimeChange, }: CalendarSlotProps) => import("react/jsx-runtime").JSX.Element;
export {};
