import type { TimeFormat } from '../../types';
type TimeSelectContentProps = {
    selected?: string;
    timeFormat?: TimeFormat;
    onChange?: (value: string) => void;
};
export declare const TimeSelectContent: ({ selected, timeFormat, onChange }: TimeSelectContentProps) => import("react/jsx-runtime").JSX.Element;
export {};
