import type { DateSlotKey } from '../types';
type CalendarSlotDateProps = {
    value: string;
    name: string;
    placeholder: string;
    slotId: DateSlotKey;
    onSelect?: () => void;
    onChange?: (value: Date) => void;
};
export declare const CalendarSlotDate: ({ slotId, value, placeholder, name, onSelect, onChange }: CalendarSlotDateProps) => import("react/jsx-runtime").JSX.Element;
export {};
