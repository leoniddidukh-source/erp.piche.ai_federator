import type { DateRange, DateSingle, TimeFormat } from './types';
type CalendarProps = {
    single?: DateSingle;
    range?: DateRange;
    isRange?: boolean;
    isDueDate?: boolean;
    hasSlots?: boolean;
    hasTime?: boolean;
    timeFormat?: TimeFormat;
    onSingleChange?: (value: Date | null) => void;
    onRangeChange?: (value: DateRange) => void;
    onClear?: () => void;
};
export declare const Calendar: ({ single, range, isRange, isDueDate, hasSlots, hasTime, timeFormat, onSingleChange, onRangeChange, onClear, }: CalendarProps) => import("react/jsx-runtime").JSX.Element;
export {};
