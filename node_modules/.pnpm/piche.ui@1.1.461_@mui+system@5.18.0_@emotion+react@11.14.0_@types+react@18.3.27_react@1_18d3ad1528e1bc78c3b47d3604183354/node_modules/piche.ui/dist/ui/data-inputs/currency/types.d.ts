/// <reference types="react" />
import type { BoxProps, InputProps } from '@mui/material';
import type { ComponentMode } from '../../types';
export interface PuiCurrencyEditProps {
    value: string;
    signed: boolean;
    defaultCurrency: string;
    wrapperSx?: BoxProps['sx'];
    inputSx?: InputProps['sx'];
    isLoading: boolean;
    onSave: (number: number) => void;
    onChange: (number: string) => void;
    disabled?: boolean;
    inputRef?: React.Ref<HTMLInputElement>;
    autoFocus?: boolean;
    placeholder?: string;
}
export interface PuiCurrencyProps extends PuiCurrencyEditProps {
    mode?: ComponentMode;
}
export interface PuiCurrencyViewProps {
    value: string;
    defaultCurrency: string;
}
