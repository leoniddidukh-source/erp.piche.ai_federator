export declare const getTooltipTitle: (selectedValues: string[]) => string;
export declare const getDropdownChipLabel: (label: string) => string;
export declare const containsText: (text: string, searchText: string) => boolean;
interface ChipVisibilityResult {
    visibleChips: string[];
    hiddenChips: string[];
    remainingCount: number;
    accumulatedWidth: number;
    isReady: boolean;
}
interface CalculateChipVisibilityParams {
    selectedValues: string[];
    parentRef: HTMLDivElement;
    currentColumnWidth: number;
}
export declare const calculateChipVisibility: ({ selectedValues, parentRef, currentColumnWidth, }: CalculateChipVisibilityParams) => ChipVisibilityResult;
export {};
