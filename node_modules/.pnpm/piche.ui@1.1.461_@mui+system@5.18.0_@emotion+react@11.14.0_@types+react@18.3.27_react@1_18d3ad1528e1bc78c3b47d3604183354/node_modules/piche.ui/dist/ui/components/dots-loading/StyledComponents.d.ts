import type { StyledComponent } from '@emotion/styled';
import type { BoxProps } from '@mui/material';
export declare const DotsContainer: StyledComponent<BoxProps>;
interface DotProps extends BoxProps {
    delay: string;
    color: string;
}
export declare const Dot: StyledComponent<DotProps>;
export {};
