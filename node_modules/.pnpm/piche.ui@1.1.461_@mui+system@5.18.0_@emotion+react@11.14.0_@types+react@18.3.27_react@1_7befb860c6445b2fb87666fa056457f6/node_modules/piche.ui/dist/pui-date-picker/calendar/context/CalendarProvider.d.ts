/// <reference types="react" />
import type { CalendarConfig, DateRange, DateSingle } from '../types';
type CalendarProviderProps = {
    children: React.ReactNode;
    single?: DateSingle;
    range?: DateRange;
    config: CalendarConfig;
    onSingleChange?: (value: Date | null) => void;
    onRangeChange?: (value: DateRange) => void;
    onClear?: () => void;
};
export declare const CalendarProvider: ({ children, config, single, range, onSingleChange, onRangeChange, onClear, }: CalendarProviderProps) => import("react/jsx-runtime").JSX.Element;
export {};
