import type { DateSlotKey, TimeFormat } from '../types';
type CalendarSlotTimeProps = {
    value: string;
    name: string;
    slotId: DateSlotKey;
    timeFormat: TimeFormat;
    onSelect?: () => void;
    onChange?: (value: string) => void;
};
export declare const CalendarSlotTime: ({ slotId, value, timeFormat, name, onSelect, onChange }: CalendarSlotTimeProps) => import("react/jsx-runtime").JSX.Element;
export {};
