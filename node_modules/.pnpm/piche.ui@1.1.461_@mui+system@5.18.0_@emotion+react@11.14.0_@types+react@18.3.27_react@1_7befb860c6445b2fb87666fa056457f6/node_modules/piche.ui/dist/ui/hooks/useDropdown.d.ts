import type { SelectChangeEvent } from '@mui/material/Select';
export interface UseDropdownProps<T> {
    open: boolean;
    options: T[];
    selectedValue?: string;
    disabled: boolean;
    onValueChange: (value: string) => void;
    onMenuClose?: () => void;
}
export declare const useDropdown: <T extends {
    id: string;
}>({ open, options, selectedValue, onValueChange, disabled, onMenuClose, }: UseDropdownProps<T>) => {
    selectedOptionIndex: number;
    handleChange: (event: SelectChangeEvent<unknown>) => void;
    handleReset: (event: React.MouseEvent) => void;
    handleArrowNavigation: (event: KeyboardEvent) => void;
};
