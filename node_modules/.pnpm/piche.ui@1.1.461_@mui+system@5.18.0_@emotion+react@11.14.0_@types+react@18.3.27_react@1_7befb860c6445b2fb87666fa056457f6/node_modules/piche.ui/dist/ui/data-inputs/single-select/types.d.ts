import type { PuiIcon } from '../../../ui';
export interface SingleSelectOption {
    id: string;
    label: string;
    icon?: PuiIcon;
    color: string;
}
export declare enum SingleSelectDisplayTypes {
    Classic = "classic",
    RoundIndicator = "round-indicator",
    IconWithText = "icon-with-text",
    OnlyText = "only-text"
}
export interface SingleSelectItemProps {
    option: SingleSelectOption;
    viewMode?: boolean;
}
