import type { SliderDisplayType } from '../types';
export interface SliderDisplayParams {
    sliderLineHeight: string;
    railBackgroundColor: string;
    border: string;
}
export declare const useSliderParams: (displayType: SliderDisplayType, mainColor?: string) => {
    sliderParams: SliderDisplayParams;
    color: string;
};
