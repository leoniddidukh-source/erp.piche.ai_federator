import type { ButtonProps } from '@mui/material';
export declare const getProgressSize: (size?: ButtonProps['size']) => number;
export interface IExtraStyle {
    button?: Record<string, string | number>;
    childrenWrap?: Record<string, string | number>;
    progress?: Record<string, string | number>;
}
export declare const getLoadingButtonStyles: (loading: boolean | undefined, variant: ButtonProps['variant']) => IExtraStyle;
